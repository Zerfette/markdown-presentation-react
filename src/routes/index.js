import Home from './Home'
import Present from './Present'

const routes = [
  { path: '/', text: 'Home', component: Home },
  { path: '/present', text: 'Present', component: Present },
]

export { routes }
